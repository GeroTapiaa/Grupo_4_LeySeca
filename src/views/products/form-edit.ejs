<!DOCTYPE html>
<html lang="en">

<%- include("../partials/head",{
    title : "Formulario de edición de producto:"
}) %>

    <body>
        <%- include('../partials/navbar') %>
            <main class="main__form-created">
                <div class="created-container">
                    <div class="title-created">
                        <h2>Estas editando:
                            <%= productEdit.name %>
                        </h2>
                    </div>
                    <form action="/products/update/<%=productEdit.id%>?_method=put" method="POST">
                        <div class="product-detail">
                            <div class="form__box">
                                <label for="name" class="label-form">Nombre del producto:</label>
                                <input type="text" id="name" name="name" placeholder="Ingrese el producto" class="input_form" value="<%= productEdit.name %> ">
                            </div>
                            <div class="form__box">
                                <label for="price" class="label-form">Precio:</label>
                                <input type="text" id="price" name="price" placeholder="Ingrese el precio del producto" class="input_form" value="<%= productEdit.price %> ">
                            </div>
                            <div class="form__box">
                                <label for="discount" class="label-form">Descuento:</label>
                                <input type="text" id="discount" name="discount" placeholder="Ingrese el descuento del producto" class="input_form" value="<%= productEdit.discount %> ">
                            </div>
                            <div class="form__box">
                                <label for="category" class="label-form">Categoria:</label>
                                <select name="category" id="category" class="input_form" value="<%= productEdit.category %> ">
                        
                                    <option value="Cerveza"<%=productEdit.category === 'Cerveza' && 'selected' %>>Cerveza</option>
                                    <option value="indumentaria"<%=productEdit.category === 'indumetaria' && 'selected' %> >indumentaria</option>
                        </select>
                            </div>
                            <div class="form__box--description">
                                <label for="description" class="label-form">Descripción:</label>
                                <textarea name="description" id="description" class="input_form"><%= productEdit.description%></textarea>
                            </div>
                            <div class="form__box-btn">
                                <button type="submit" class="btn__product">Guardar producto</button>


                            </div>

                        </div>
                    </form>
                </div>
            </main>
            <%- include('../partials/footer') %>
    </body>

</html>